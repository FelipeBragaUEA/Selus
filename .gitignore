# Node modules
node_modules/
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp/
.pnp.js

# Build artifacts
build/
dist/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# React-specific
.eslintcache
coverage/

# Hardhat/Truffle artifacts (not used, but good practice if you ever install them)
artifacts/
cache/

# Remix IDE specific files (if you save workspace locally - optional)
# .git/ (This line should usually NOT be here, as .git is the repository itself. Remove if present)
.vscode/